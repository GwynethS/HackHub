import { HeadlineDirective } from './headline.directive';

describe('HeadlineDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadlineDirective();
    expect(directive).toBeTruthy();
  });
});
